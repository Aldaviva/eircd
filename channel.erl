-module(channel).
-export([create/1, find/1, save/1, delete/1, add_user/2]).
-include("constants.hrl").

create(Name) ->
	unimplemented.

find(Name) ->
	unimplemented.

save(Channel) ->
	unimplemented.

delete(Channel) ->
	unimplemented.

add_user(Channel, User) ->
	unimplemented.